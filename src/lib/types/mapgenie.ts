// Barrel file for shared frontend types.
//
// Split out into game.ts for the modularity assignment; re-exported here so the
// many `import type { Game } from '.../types/mapgenie'` lines keep working.

export * from './game';
